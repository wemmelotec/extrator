document.addEventListener("DOMContentLoaded", function () {
    // Arquivo reservado para comportamentos da tela de consulta.
    // Nesta etapa, os filtros ainda nao executam consulta no backend.
});
